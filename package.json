{
  "name": "discord-bot",
  "version": "1.0.0",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "build": "mkdir -p src/commands src/models && npm install"
  },
  "dependencies": {
    "discord.js": "^14.13.0",
    "mongoose": "^8.0.3",
    "dotenv": "^16.3.1"
  }
}
